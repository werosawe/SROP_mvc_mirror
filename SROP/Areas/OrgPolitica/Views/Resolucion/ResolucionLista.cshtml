@model IEnumerable<BE_Resolucion>
@{ 
    int rowIndex = 0;
}    
@if (Model.Count() > 0)
{
    <table id="tblEtapas" class="table table-bordered mb-0">
        <thead class="thead-default">
            <tr>
                <th>Eliminar</th>
                <th>N°</th>
                <th>N° Resolución</th>
                <th>Fecha Resolución</th>
                <th>Fecha de elevación</th>
                <th>Ver</th>
            </tr>
        </thead>
        <tbody>
            @foreach (BE_Resolucion item in Model)
            {
                rowIndex = rowIndex + 1;
                <tr>
                    <td>
                        <button type="button" data-codigo='{"Orden": @item.Orden, "Cod_OP": @item.Cod_OP}' title='¿Eliminar?'  class="btn btn-icon btn-pure danger" data-toggle="eliminaetapa" data-placement="top">
                            <i class="icon-android-delete"></i>
                        </button>
                    </td>
                    <td>@rowIndex.CerosIzquierda(3)</td>
                    <td><a class="editEtapa" data-toggle="modal" data-target="#WinComun" href="#" data-codigo='{"Orden": @item.Orden, "Cod_OP": @item.Cod_OP, "ESTADOPAGINA": "@enumEstadoPagina.Edicion"}' >@item.Des_Doc</a></td>
                    <td>@item.Fec_Doc.Text("dd/MM/yyyy")</td>
                    <td>@item.Fec_Eleva.Text("dd/MM/yyyy")</td>
                    <td></td>
                    @*@{if (rowIndex > 1) {
                                <td><button type="button" data-toggle="modal" data-target="#WinComun" class="btn btn-icon btn-pure insertEtapa" data-codigo='{"Cod_Correlativo": @item.Cod_Correlativo, "Cod_OP": @item.Cod_OP , "ESTADOPAGINA" : "@enumEstadoPagina.Insertar" }' title='@string.Concat("Insertar Etapa entre ", item.Cod_Correlativo - 1, " y ", item.Cod_Correlativo)' ><i class="icon-plus-square-o"></i></button></td>
                         }
                    }*@
                </tr>
            }
        </tbody>
    </table>
}
@Glo.Paginador()